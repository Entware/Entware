--- a/src/opkg-cl.c
+++ b/src/opkg-cl.c
@@ -256,7 +256,7 @@ static int args_parse(int argc, char *ar
 	}
 
 	if (!conf->conf_file && !conf->offline_root)
-		conf->conf_file = xstrdup("/etc/opkg.conf");
+		conf->conf_file = xstrdup("/opt/etc/opkg.conf");
 
 	if (parse_err)
 		return parse_err;
